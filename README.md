steps to build the app

initialized the app with npm create vite@latest app name -- --template react
-remove unwanted loc
-run application using -npm run dev
-link google fonts

installing third party libraries
..................................
-react bootstrap
-material UI
